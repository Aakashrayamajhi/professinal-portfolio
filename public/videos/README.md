Drop your project preview clips here, named to match `lib/data.js`:

- skysearch.mp4
- e-mistiri.mp4

Keep clips short (10-20s), muted-friendly, and under a few MB each for fast loading.
Until a file exists at a given path, that project card gracefully falls back to a
solid color panel with the project name instead of a broken video.
